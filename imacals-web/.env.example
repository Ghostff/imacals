PORT=5175
VITE_API_BASE=http://localhost:3032/api
VITE_API_TARGET=http://imacals-api:3032

# Serve the sample catalogue in src/services/catalog.ts instead of calling /catalog.
# The products and orders endpoints do not exist yet (docs/business_logic.md §3), so this stays
# true until they ship. While it is on, a banner tells visitors the products are samples.
VITE_USE_PREVIEW_CATALOG=true
